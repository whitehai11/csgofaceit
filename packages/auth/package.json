{
  "name": "@csgofaceit/auth",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "typecheck": "npx tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/jwt": "^9.0.1",
    "fastify-plugin": "^5.0.1"
  },
  "peerDependencies": {
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
